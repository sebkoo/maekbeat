{
  "name": "@maekbeat/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Maekbeat API server: WebSocket vitals ingest, per-device ring-buffer store, REST reads, OpenAPI",
  "license": "Apache-2.0",
  "//exports": "Not a public API. These three modules are read by infra/cdk so the deployment's environment contract, its load-balancer idle timeout and its health-check path are derived from the server rather than restated beside it; nothing else in the workspace imports this package.",
  "exports": {
    "./app": "./src/app.ts",
    "./config": "./src/config.ts",
    "./stream": "./src/stream.ts"
  },
  "scripts": {
    "demo": "tsx scripts/demo.ts",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/swagger": "^9.4.0",
    "@fastify/swagger-ui": "^5.2.0",
    "@fastify/websocket": "^11.0.0",
    "@maekbeat/protocol": "workspace:*",
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/exporter-trace-otlp-http": "^0.221.0",
    "@opentelemetry/resources": "^2.10.0",
    "@opentelemetry/sdk-trace-base": "^2.10.0",
    "@opentelemetry/sdk-trace-node": "^2.10.0",
    "@opentelemetry/semantic-conventions": "^1.43.0",
    "fastify": "^5.2.0",
    "tsx": "^4.19.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@maekbeat/vitals-sim": "workspace:*",
    "@types/node": "^26.1.2",
    "@types/ws": "^8.5.0",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "ws": "^8.18.0"
  }
}
